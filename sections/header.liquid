<div class="bg-zinc-500">
    <h1 class="text-3xl font-bold text-white text-center">{{ shop.name | upcase }}</h1>
    {% for link in linklists.main-menu.links %}
        <a href="{{ link.url }}">{{ link.title }}</a>
    {% endfor %}
</div>
